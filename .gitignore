# Reference-only sources — never build or commit
OpenJK/
JKA_YBEProxy/

# Local original JA+ lib, engine binaries + build output (never commit)
jampgamei386.so
japlus_real_i386.so
linuxjampded
libcxa.so.1
proxy/japlusproxy.so
proxy/build-i686/
